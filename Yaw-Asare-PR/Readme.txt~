Code is written in Ruby
Uses sinatra rest api

To Install use commands:

	apt-get install ruby-full build-essential
	apt-get install rubygems
	gem install sinatra
	gem install json
	gem install thin

to run server use following commands in /source-code/:
	ruby VIN-SERVER

to run test use following commands in /source-code/unit-test/
	ruby *[testName]


